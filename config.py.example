# Killfeed Bot Configuration (killfeed_bot.py)
KILLFEED_BOT_TOKEN = "YOUR_DISCORD_BOT_TOKEN_HERE"

# --- Database Paths ---
RANKING_DB_PATH = "/home/steam/bots/Killfeed/ranking.db"
SPAWNS_DB_PATH = "/path/to/your/spawns.db"

# --- Bot Behavior ---
# A dictionary mapping a server's name to a boolean.
# True = Post only PvP deaths. False = Post all deaths.
PVP_ONLY_DEATHS = {
    "Exiled Lands": False,
    "Siptah": False,
    # "PvP": True,
}

# --- Unified Ranking Configuration (Optional) ---
# Create unified leaderboards that aggregate scores from multiple servers.
# Useful for clusters where players travel between maps.
# UNIFIED_RANKINGS = [
#     {
#         "enabled": True,
#         "title": "🏆 Cluster Ranking (All Servers)",
#         "channel_id": 123456789012345684, # Dedicated channel for this ranking
#         "servers_to_include": ["Exiled Lands", "Siptah"], # Must match "name" in SERVERS list
#         "update_interval": 300, # Update every 5 minutes
#     }
# ]

# --- Server Configurations ---
# This is a list of dictionaries, where each dictionary represents a server you want to monitor.
SERVERS = [
    {
        "name": "Exiled Lands",
        "enabled": True,
        "channel_id": 123456789012345678,
        "ranking_channel_id": 123456789012345679, # Channel for the auto-updating ranking message
        "saved_path": "/path/to/your/conan/saved/folder/",
        "db_pattern": "game_backup_*.db",
        "last_event_file": "/path/to/your/last_event_time.txt",
        "poll_interval": 20, # How often to check for new kills
        "ranking_update_interval": 300, # How often (in seconds) to update the ranking message
    },
    {
        "name": "Siptah",
        "enabled": True,
        "channel_id": 123456789012345680,
        "ranking_channel_id": 123456789012345681,
        "saved_path": "/path/to/your/siptah/saved/folder/",
        "db_pattern": "dlc_siptah_backup_*.db",
        "last_event_file": "/path/to/your/siptah_last_event_time.txt",
        "poll_interval": 25,
        "ranking_update_interval": 300,
    },
    # {
    #     "name": "PvP",
    #     "enabled": True,
    #     "channel_id": 123456789012345682,
    #     "ranking_channel_id": 123456789012345683,
    #     "saved_path": "/path/to/your/pvp/saved/folder/",
    #     "db_pattern": "game_backup_*.db",
    #     "last_event_file": "/path/to/your/pvp_last_event_time.txt",
    #     "poll_interval": 20,
    #     "ranking_update_interval": 300,
    # },
]